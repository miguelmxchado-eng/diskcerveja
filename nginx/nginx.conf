events {}

http {
    resolver 127.0.0.11 ipv6=off valid=10s;
    map $http_upgrade $connection_upgrade {
        default upgrade;
        ""      close;
    }

    # HTTP: desafio ACME (Let's Encrypt) + redireciona o resto para HTTPS
    server {
        listen 80 default_server;
        listen [::]:80 default_server;
        server_name emporiomachado.duckdns.org;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    server {
        listen 443 ssl default_server;
        listen [::]:443 ssl default_server;
        server_name emporiomachado.duckdns.org;

        ssl_certificate     /etc/letsencrypt/live/emporiomachado.duckdns.org/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/emporiomachado.duckdns.org/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers off;

        add_header Permissions-Policy "camera=(self), microphone=()" always;
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

        location / {
            proxy_pass http://backend:8080;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_read_timeout 60s;
        }
    }
}
